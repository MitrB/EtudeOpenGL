#pragma once
#include "ecs.h"
#include "structs.hpp"

class Physics : public System {
    public:
        void update(Update update) {
            for (Entity entity : entities) {
            } 
        }

};